import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ProfilePicker = () => {
  return (
    <View>
      <Text>ProfilePicker</Text>
    </View>
  );
};

export default ProfilePicker;

const styles = StyleSheet.create({});
